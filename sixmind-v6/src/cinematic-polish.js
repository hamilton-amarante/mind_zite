(()=>{const nav=document.querySelector('.nav');const links=[...document.querySelectorAll('.nav nav a')];const sections=[...document.querySelectorAll('main section[id]')];let ticking=false;const update=()=>{ticking=false;const y=scrollY;nav?.classList.toggle('nav-scrolled',y>40);let current='';sections.forEach(s=>{const r=s.getBoundingClientRect();if(r.top<=innerHeight*.42&&r.bottom>=innerHeight*.42)current=s.id});links.forEach(a=>a.classList.toggle('is-current',a.getAttribute('href')==='#'+current));const p=Math.min(1,Math.max(0,y/(document.documentElement.scrollHeight-innerHeight||1)));document.documentElement.style.setProperty('--page-progress',p.toFixed(4))};addEventListener('scroll',()=>{if(!ticking){ticking=true;requestAnimationFrame(update)}},{passive:true});update();document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>{window.SixMindAnalytics?.track?.('anchor_click',{target:a.getAttribute('href')||''})}));})();