const cal=document.getElementById('calendarGrid');if(cal&&!cal.children.length){for(let i=1;i<=30;i++){const s=document.createElement('span');s.textContent=i;if(i===17)s.className='active';cal.appendChild(s)}}const journey=document.getElementById('journey'),scenes=[...document.querySelectorAll('.platform-scene')],nav=[...document.querySelectorAll('.journey-nav button')];function updateJourney(){if(!journey)return;const r=journey.getBoundingClientRect(),total=Math.max(1,journey.offsetHeight-innerHeight),p=Math.min(1,Math.max(0,-r.top/total));document.documentElement.style.setProperty('--progress',p.toFixed(4));const idx=Math.min(4,Math.floor(p*5.001));scenes.forEach((s,i)=>s.classList.toggle('active',i===idx));nav.forEach((b,i)=>b.classList.toggle('active',i===idx))}addEventListener('scroll',()=>requestAnimationFrame(updateJourney),{passive:true});updateJourney();nav.forEach((b,i)=>b.addEventListener('click',()=>{if(!journey)return;const start=scrollY+journey.getBoundingClientRect().top,total=journey.offsetHeight-innerHeight;scrollTo({top:start+total*(i+.05)/5,behavior:'smooth'})}));