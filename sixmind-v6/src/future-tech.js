(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduce)return;const css=document.createElement('link');css.rel='stylesheet';css.href='src/f16-f20.css';document.head.appendChild(css);const canvas=document.createElement('canvas');canvas.className='future-canvas';canvas.setAttribute('aria-hidden','true');document.body.prepend(canvas);const layer=document.createElement('div');layer.className='future-layer';document.body.prepend(layer);const scan=document.createElement('div');scan.className='scanline';document.body.prepend(scan);const isLow=matchMedia('(max-width: 700px)').matches||(navigator.hardwareConcurrency&&navigator.hardwareConcurrency<=4);import('https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js').then(THREE=>{const renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:!isLow,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio||1,isLow?1:1.5));renderer.setSize(innerWidth,innerHeight,false);const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(58,innerWidth/innerHeight,.1,100);camera.position.z=7.2;const group=new THREE.Group();scene.add(group);const count=isLow?650:1500;const geo=new THREE.BufferGeometry();const pos=new Float32Array(count*3);for(let i=0;i<count;i++){const r=2+Math.random()*4.8,a=Math.random()*Math.PI*2,b=(Math.random()-.5)*Math.PI;pos[i*3]=Math.cos(a)*Math.cos(b)*r;pos[i*3+1]=Math.sin(b)*r*.72;pos[i*3+2]=Math.sin(a)*Math.cos(b)*r}geo.setAttribute('position',new THREE.BufferAttribute(pos,3));const mat=new THREE.PointsMaterial({color:0xd6a32a,size:isLow?.018:.026,transparent:true,opacity:.45,depthWrite:false,blending:THREE.AdditiveBlending});const pts=new THREE.Points(geo,mat);group.add(pts);const torus=new THREE.Mesh(new THREE.TorusGeometry(2.65,.012,6,180),new THREE.MeshBasicMaterial({color:0xf4d47c,transparent:true,opacity:.14,blending:THREE.AdditiveBlending}));torus.rotation.x=1.15;torus.rotation.z=.2;group.add(torus);const torus2=torus.clone();torus2.scale.set(.72,.72,.72);torus2.rotation.x=.35;torus2.rotation.y=1.1;group.add(torus2);let mx=0,my=0,sy=0;addEventListener('pointermove',e=>{mx=e.clientX/innerWidth-.5;my=e.clientY/innerHeight-.5},{passive:true});addEventListener('scroll',()=>{sy=scrollY/(document.documentElement.scrollHeight-innerHeight||1)},{passive:true});const resize=()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight,false)};addEventListener('resize',resize,{passive:true});let running=true;document.addEventListener('visibilitychange',()=>running=!document.hidden);const clock=new THREE.Clock();const animate=()=>{requestAnimationFrame(animate);if(!running)return;const t=clock.getElapsedTime();group.rotation.y=t*.035+mx*.18;group.rotation.x=Math.sin(t*.12)*.05+my*.08;group.position.y=-sy*.7;torus.rotation.z=t*.08;torus2.rotation.z=-t*.05;renderer.render(scene,camera)};animate()}).catch(()=>{canvas.remove()});})();